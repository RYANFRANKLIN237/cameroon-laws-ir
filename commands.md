#  mac command for .venv : source .venv/bin/activate
# update requirements.txt : pip freeze > requirements.txt
# run the app : python main.py
# format python code : command + ]
# format html  & js code : Shift + Option + F
